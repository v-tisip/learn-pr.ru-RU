<span data-ttu-id="f4888-101">В качестве образа для создания виртуальной машины мы использовали `Debian`.</span><span class="sxs-lookup"><span data-stu-id="f4888-101">We used `Debian` for the image to create the virtual machine.</span></span> <span data-ttu-id="f4888-102">В Azure есть несколько стандартных образов виртуальных машин, на основе которых можно создать виртуальную машину.</span><span class="sxs-lookup"><span data-stu-id="f4888-102">Azure has several standard VM images you can use to create a virtual machine.</span></span> 

<span data-ttu-id="f4888-103">Список доступных образов можно получить с помощью команды `az vm image list --output table`.</span><span class="sxs-lookup"><span data-stu-id="f4888-103">You can get a list of the available images using the `az vm image list --output table` command.</span></span> <span data-ttu-id="f4888-104">Она выводит наиболее популярные образы, встроенные в Azure CLI.</span><span class="sxs-lookup"><span data-stu-id="f4888-104">This will output the most popular images that are part of an offline list built into the Azure CLI.</span></span> <span data-ttu-id="f4888-105">Однако в Azure Marketplace доступны _сотни_ других вариантов.</span><span class="sxs-lookup"><span data-stu-id="f4888-105">However, there are _hundreds_ of image options available in the Azure Marketplace.</span></span> 

> [!TIP]
> <span data-ttu-id="f4888-106">Чтобы получить полный список, добавьте к команде флаг `--all`.</span><span class="sxs-lookup"><span data-stu-id="f4888-106">You can get a full list by adding the `--all` flag to the command.</span></span> <span data-ttu-id="f4888-107">Так как список образов в Marketplace очень велик, полезно будет отфильтровать его с помощью параметра `--publisher` или `–-offer`.</span><span class="sxs-lookup"><span data-stu-id="f4888-107">Since the list of images in the Marketplace is very large, it is helpful to filter the list with the `--publisher` or `–-offer` options.</span></span>

<span data-ttu-id="f4888-108">Некоторые образы доступны только в определенных регионах.</span><span class="sxs-lookup"><span data-stu-id="f4888-108">Some images are only available in certain locations.</span></span> <span data-ttu-id="f4888-109">Чтобы результаты содержали только те образы, которые доступны в регионе, где будет создаваться виртуальная машина, добавьте к команде флаг `--location [location]`.</span><span class="sxs-lookup"><span data-stu-id="f4888-109">Try adding the `--location [location]` flag to the command to scope the results to ones available in the region where you want to create the virtual machine.</span></span> <span data-ttu-id="f4888-110">Например, чтобы получить список образов, доступных в регионе `eastus`, введите в Azure Cloud Shell приведенную ниже команду.</span><span class="sxs-lookup"><span data-stu-id="f4888-110">For example, type the following into Azure Cloud Shell to get a list of images available in the `eastus` region.</span></span>

```azurecli
az vm image list --location eastus --output table
```

> [!NOTE]
> <span data-ttu-id="f4888-111">Это стандартные образы, предоставляемые Azure.</span><span class="sxs-lookup"><span data-stu-id="f4888-111">These are the standard images that are provided by Azure.</span></span> <span data-ttu-id="f4888-112">Имейте в виду, что вы также можете [создавать и отправлять собственные образы](https://docs.microsoft.com/azure/virtual-machines/linux/tutorial-custom-images) для создания виртуальных машин с использованием уникальных конфигураций либо менее распространенных версий или дистрибутивов операционных систем.</span><span class="sxs-lookup"><span data-stu-id="f4888-112">Keep in mind that you can also [create and upload your own custom images](https://docs.microsoft.com/azure/virtual-machines/linux/tutorial-custom-images) to create VMs based on unique configurations or less common versions or distributions of an operating system.</span></span>